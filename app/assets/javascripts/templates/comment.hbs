<li class="comment" id="comment_{{id}}">
  <p>
    <span class="comment-body">{{body}}</span>
    <small class="comment-poster"> 
      — {{author}}, {{created}} ago  
      {{#if edited}}
      <span class="text-muted"> edited {{edited}}</span>
      {{/if}}
      <a class="edit-comment" href="#">edit</a> <a class="delete-comment" data-confirm="Are you sure?" data-method="delete" data-remote="true" href="/{{commentable}}/{{commentable_id}}/comments/{{id}}" rel="nofollow" data-id="{{id}}">delete</a></small></p>
</li>
