<% if @answer.errors.any? %>
  Underflow.question.renderAnswerForm("<%= j render 'answers/form' %>")
<% else %>
  Underflow.question.addAnswer("<%= j render @answer, has_best_answer: @answer.question.has_best_answer? %>")
<% end %>
